#wrapper.gray-bg
  .wrapper.wrapper-content.animated.fadeInRight.center-block
    .col-xs-12
      .ibox
        .ibox-content
          %h1
            .fa.fa-user
            Pagamentos
          %fieldset{:style => "height: auto"}
            - flash.each do |name, msg|
              %p
                = content_tag :div, msg.html_safe, class: name
            .row
              .col-xs-12.b-r
                .table-responsive
                  %table.table.table-striped
                    %thead
                      %tr
                        %th Nome do cliente
                        %th Comitê local
                        %th Programa
                        %th Valor
                        %th Status
                    %tbody
                      - payments.each do |payment|
                        %tr
                          %td= payment.customer_name
                          %td= payment.local_committee
                          %td= payment.program
                          %td= payment.value
                          %td= payment.status
